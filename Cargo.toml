[package]
name = "tinosrse-tools"
version = "0.2.1"
edition = "2024"
description = "TinosRSE tools is a CLI tool suite for compiling TinosRSE."
license = "MIT"
readme = "README.md"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
cargo_metadata = "0.15"
serde_json = "1.0.151"
